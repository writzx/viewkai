[package]
name = "viewkai-engine"
version = "0.1.1"
edition.workspace = true
license.workspace = true
repository.workspace = true
authors.workspace = true
description = "PDFium rendering engine for viewkai"
readme = "../../README.md"
keywords = ["pdf", "pdfium", "rendering", "viewer", "wasm"]
categories = ["graphics", "rendering", "wasm"]

[dependencies]
viewkai-core = { path = "../viewkai-core" }
pdfium-render = { workspace = true, features = ["pdfium_latest", "image_latest"] }
thiserror = { workspace = true }

[dev-dependencies]
serde_json = { workspace = true }

[lints]
workspace = true
