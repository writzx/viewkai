[package]
name = "viewkai"
version = "0.2.1"
edition.workspace = true
license.workspace = true
repository.workspace = true
authors.workspace = true
description = "Rust PDF viewer library targeting wasm32 and native, built on pdfium-render and egui"
readme = "../../README.md"
keywords = ["pdf", "egui", "viewer", "wasm", "gui"]
categories = ["gui", "rendering", "wasm"]

[dependencies]
viewkai-core = { path = "../viewkai-core" , version = "0.2.1" }
viewkai-engine = { path = "../viewkai-engine" , version = "0.2.1" }
viewkai-plugins = { path = "../viewkai-plugins" , version = "0.2.1" }
egui = { workspace = true }

[dev-dependencies]
viewkai-engine = { path = "../viewkai-engine" , version = "0.2.1" }
criterion = { version = "0.5", features = ["html_reports"] }
egui_kittest = { version = "0.34", features = ["wgpu", "snapshot", "eframe"] }

[[bench]]
name = "large_doc"
harness = false

[lints]
workspace = true
